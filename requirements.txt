fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlmodel>=0.0.22
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.1
python-multipart>=0.0.9
jinja2>=3.1.4
httpx>=0.27.2
pypdf>=4.3.1
scikit-learn>=1.5.1
litellm>=1.48.0
python-dotenv>=1.0.1

email-validator>=2.2.0

# Postgres driver -- required in production (e.g. Vercel), where SQLite's local
# file storage doesn't persist between serverless invocations. Works with a
# plain "postgresql://" DATABASE_URL (SQLAlchemy's default dialect driver).
# Not needed for local dev if DATABASE_URL stays sqlite.
psycopg2-binary>=2.9.9
